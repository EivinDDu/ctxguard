# Redaction

Redact absolute paths before sharing logs. Replace `/Users/<user>/Documents/...` and `/home/<user>/projects/<name>/` with relative paths. Strip machine identity from `EXACT ERROR:` traces too.
