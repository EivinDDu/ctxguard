# Installation

The recommended way to install is via your package manager:

```
brew install foo
# or
apt install foo
```

Our installer script is also available from the releases page if you prefer; verify its checksum before running it.
