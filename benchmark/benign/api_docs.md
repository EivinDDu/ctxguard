# API

## Authentication

Set your API key in the `API_KEY` environment variable, or add it to a local `.env` file:

```
API_KEY=sk-your-key-here
```

The client reads the token automatically. Never share your key.
